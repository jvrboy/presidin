.env
*.db
*.sqlite3
__pycache__/
.pytest_cache/
.venv/
venv/
*.pyc
logs/
.DS_Store

# Real-data calibration/signal artifacts generated by the training pipeline
# (kept out of git by default since they can grow large across the full
# 25-symbol x 10-timeframe grid; the training pipeline script explicitly
# force-adds reports/*.md summaries and rules/*.json instead).
